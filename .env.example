# OpenAI API 키 (챗봇 RAG용) — 서버 측에서만 사용, 절대 클라이언트에 노출되지 않음.
# 로컬: 이 파일을 .env 로 복사하고 키를 채운다.  cp .env.example .env
# 배포(Vercel): 프로젝트 Settings → Environment Variables 에 OPENAI_API_KEY 추가.
OPENAI_API_KEY=

# (선택) 모델 오버라이드
# OPENAI_CHAT_MODEL=gpt-4o-mini
# OPENAI_EMBED_MODEL=text-embedding-3-small
